import { atom } from 'jotai'

export const isCityCameraAnimation02AnimatingAtom = atom<boolean>(false);

export const cityCameraAnimation02ScrollDirAtom = atom<number | undefined>(undefined);

export const cityCameraAnimation02ProgressAtom = atom<number>(0);